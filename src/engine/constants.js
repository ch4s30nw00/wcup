// engine/constants.js — CALC_SPEC v2.1 확정 초기값 (산식확정 보고서 §6 + 데이터담당 요청사항 반영).
// 모든 매직넘버는 이 한 파일에 모은다. 전부 밸런싱 대상(플레이테스트로 조정).
// 변수(거리·각도·스탯 등)는 여기 없다 — 보드·players.json에서 실시간 계산. 여기엔 가중치만.
//
// v2 → v2.1 변경 요약:
//  - 스탯 스케일: 0~100 → FM 1~20 (데이터담당 CSV 이관, 보고서 부록 A 어댑터 교체)
//  - 수비 스탯 스케일: 압박 β를 수비수별(위치선정·마크·태클 + 예측력)로 스케일 (보고서 §4.4 보류분 해제)
//  - 중거리(longshots): 거리 감쇠 완화 계수 — "중거리는 거리값에서 빠지는 값 적어지게" (데이터 요청 §8)
//  - 헤더 슛: 골결:헤더 = 3:7 + 공중 듀얼(점프+키+몸싸움) (데이터 요청 §2·3)
//  - 드리블 몸싸움 듀얼: 몸싸움+균형 상시 보정 (데이터 요청 §5)
//  - SEQ.FLOW 축소: 수비 붕괴는 이제 수비수 위치 재계산(defense.js)이 주 메커니즘, FLOW는 잔여 기세 보정

export const K = {
  // 전역
  STAT_FLOOR: 0.3, // norm() 하한 — 저능력도 최소 기여 (게임디자인 floor, 보고서 부록 B)
  P_MIN: 0.02, // 전역 clamp — 0%·100% 없음 (기적·실수의 여지)
  P_MAX: 0.97,
  GOAL: { x: 120, y: 40, postA: 43.66, postB: 36.34 },

  // 스탯 어댑터 — FM 1~20 (부록 A: norm(v) = 0.3 + 0.7·v/20, 출력 [0.335, 1.0])
  STAT: {
    FM_MAX: 20,
    MID: 0.7, // 스킬·수비 항 공통 중심점 (FM ≈ 11.4 = 월드컵 출전급 평균)
    DEF_GAIN: 1.5, // 수비 스탯 → 압박 β 스케일 기울기
    DEF_MIN: 0.75, // 스케일 하한 (스탯 최저여도 압박의 3/4는 유지)
    DEF_MAX: 1.3, // 스케일 상한
    H_MIN: 165, // 키 정규화 구간 (165~200cm → [0.3, 1.0])
    H_RANGE: 35,
  },

  // 이동/공 속도 (피치 단위 ≈ m/s) — 연출(playback)과 수비 재배치 시간 예산이 공유
  // 로빙은 일반 패스보다 체공 시간이 있어 약간 느리다. 판정·수비 이동·재생이
  // 같은 속도를 사용해야 화면과 확률이 어긋나지 않는다.
  SPEED: {
    run: 9,
    dribble: 6.5,
    pass: 22,
    lob: 16.5,
    shot: 30,
  },

  // Through passes are not a single fixed-speed kick. The engine chooses a
  // speed from the runner/ball arrival time, but the minimum rises with pass
  // distance: a far ball needs more force and cannot roll as slowly as a
  // close one.
  THROUGH: {
    GROUND_MAX: 22,
    GROUND_NEAR_MIN: 9,
    GROUND_FAR_MIN: 16,
    LOB_MAX: 16.5,
    LOB_NEAR_MIN: 7.5,
    LOB_FAR_MIN: 12.5,
    FAR_DISTANCE: 55,
    FALLBACK_GROUND: 13.5,
    FALLBACK_LOB: 12,
  },

  // 궤적 곡률 상한 (geometry.clampCtrl) — 공도 사람도 무한히 휘지 않는다.
  // 척도는 새지타(sagitta): 현(직선)의 중점에서 곡선 중점까지의 거리 = 곡률 핸들을 끈 깊이.
  //
  //   RATIO   액션별 최대 휨을 **현 길이 대비 비율**로. 절대 상한(m)을 두지 않으므로
  //           가까우면 조금, 멀수록 그만큼 크게 휜다 (사용자 확정 2026-07-27).
  //           0.33 = "뚜렷한 활" — 20m는 6.6m, 45m는 14.9m까지. 반원(0.5)의 2/3 수준이라
  //           크게 도는 게 확실히 보이되 궤적이 접히진 않는다.
  //           휜 만큼 실제 비행 거리가 길어져 성공률로 대가를 치른다(패스 B_LEN, 슛 bend).
  //   MIN_M   아주 짧은 액션도 이만큼은 휠 수 있게 (비율만 두면 3m 패스는 직선 강제)
  //   ALONG   현 **방향**(앞뒤)으로 핸들을 끌 수 있는 범위. 패스·슛은 0 —
  //           공의 궤적 조절점은 가운데 고정이고 좌우(휨)로만 움직인다.
  //           앞뒤로 끌면 궤적이 접혀 고리가 생기는데, 그건 공이 날아가는 모양이 아니다.
  //           드리블·오프볼 런은 사람이 달리는 경로라 앞뒤 치우침을 조금 허용한다.
  //   SKILL_* 감아 차는 폭은 사람마다 다르다 — 벽을 넘겨 감는 건 기술이다.
  //           예전엔 RATIO가 순수 기하라서 중앙수비수도 브루누처럼 감았다.
  //           크로스(공에 회전을 먹여 휘게 하는 능력) 6 : 발재간 4로 섞어 상한을 조절한다.
  //           중립 스탯에서 계수 1.0 = 지금까지의 상한 그대로라 평균 선수는 밸런스가 안 바뀐다.
  //           공을 차는 액션(패스·슛)에만 적용한다 — 드리블·오프볼 런은 사람이 달리는
  //           경로라 발기술이 아니라 몸의 문제다.
  //           휨의 **대가**(호가 길어져 성공률이 깎이는 것)는 그대로 둔다. 여기서 바꾸는 건
  //           "얼마나 감을 수 있는가"지 "감으면 얼마나 손해인가"가 아니다.
  BEND: {
    RATIO: { pass: 0.33, shot: 0.33, dribble: 0.25, run: 0.25 },
    MIN_M: 1.2,
    ALONG: { pass: 0, shot: 0, dribble: 0.3, run: 0.3 },
    SKILL_MIX: { crossing: 0.6, flair: 0.4 },
    SKILL_KINDS: { pass: true, shot: true },
    SKILL_GAIN: 1.5,
    SKILL_MIN: 0.65, // 발기술이 없어도 최소한은 감긴다
    SKILL_MAX: 1.35,
    //   COST_*  감은 만큼 치르는 대가를 기술로 조절한다.
    //           상한(SKILL_*)만으로는 결과가 거의 안 바뀐다 — 호가 길어지는 비용이 이미
    //           휨을 비슷한 지점에서 묶고 있어서, 최적 휨이 상한과 나란히 커지기 때문이다.
    //           (측정: 8~45m 일곱 길이 전부에서 월드클래스와 중앙수비수의 차이가 0.0%p)
    //           그래서 "휘어진 만큼 늘어난 거리"에 계수를 곱한다. 발기술이 좋으면 같은
    //           휨을 더 싸게 치고, 없으면 더 비싸게 친다.
    //           직선 궤적이면 늘어난 거리가 0이라 계수가 무엇이든 영향이 없다 —
    //           앵커는 전부 직선이므로 그대로 유지된다.
    //           크기는 조준(SHOT.AIM_GAIN)과 같은 급으로 맞췄다 — 25m 패스에서 레인 수비수를
    //           감아 넘길 때 월드클래스와 중앙수비수의 차이가 3.3%p (조준은 손흥민 2.5%p).
    //           부수적 장치라 이보다 크면 과하다 (2.4면 5.0%p까지 벌어진다).
    COST_GAIN: 1.6,
    COST_MIN: 0.6,
    COST_MAX: 1.5,
  },

  // 연출 전용 (playback.js) — 판정에 영향 없음.
  // PASS_OVERRUN: 아무도 안 건드린 패스 실패 = "빠진 패스". 목표 지점을 지나쳐
  //   이만큼 더 굴러간다 (사용자 요청 "공이 쭉 흐르는 애니메이션").
  PLAY: {
    PASS_OVERRUN: 16,
    ACTION_LINK_MS: 0,
    ACTION_MIN_MS: 400,
    // 0~1의 화면 전용 높이. 로빙 공은 포물선 꼭대기에서 더 작게 그린다.
    LOB_HEIGHT: 1,

    // 지시가 없는 선수(앰비언트)의 이동 의도. 최종 속도는
    //   min(runSpeedOf(선수), runSpeedOf(선수) × INTENT × urgency)
    // 이라 **누구도 자기 주력을 넘지 못한다**. 예전에는 스탯과 무관한 상수(7.5·5.5…)에
    // urgency 1.9가 곱해져 라인 조정만으로도 10.4 m/s가 나왔다 — 볼트가 라인을 맞추는 꼴.
    // 값은 "그 상황에서 전력의 몇 할로 뛰는가"다: 라인 조정은 조깅, 지원 런은 전력에 가깝다.
    INTENT: {
      SHAPE: 0.35, // 라인 업다운·볼사이드 시프트 (조깅)
      SUPPORT: 0.85, // 지원 런 — 패스 옵션 만들기
      PRESS: 0.9, // 압박
      RECOVER: 0.8, // 판정 좌표(defWaypoint)로 복귀
      SHADOW: 0.55, // 복귀 후 셰도잉 잔움직임
      RESET: 0.4, // 공수 전환·대형 복귀
      CELEBRATE: 0.6, // 골 세리머니
      DEJECT: 0.25, // 실점 후 낙담
      MEET: 0.95, // 약속 지점(패스 받을 자리)으로 이동 — 늦으면 안 된다
      GK_DIVE: 1.6, // 골키퍼 다이브는 달리기가 아니라 몸을 던지는 동작
    },

    // 스크립트 세그먼트를 기다리거나 마친 선수가 "숨 쉴" 반경(m).
    // 0으로 못 박으면 석상이 되고(베타테스트 지적), 크게 주면 다음 액션의
    // 계획 좌표와 어긋나 공이 빈 잔디에 떨어진다. 그 사이의 타협값.
    LEASH_WAIT: 0.8, // 세그먼트 출발을 기다리는 중
    LEASH_HOLD: 2, // 런을 마치고 다음 액션을 기다리는 중
    LEASH_LOCK_MS: 1500, // 소비 액션이 이만큼 앞으로 다가오면 목줄을 0으로 조인다
    // 지시 출발 직전에는 목줄을 미리 0으로 조여 정확히 출발점에 서 있게 한다.
    // 안 그러면 출발 프레임에 목줄만큼(≤0.8m)을 한 번에 메우느라 속도가 튄다.
    WAIT_LOCK_MS: 500,

    // 최후 안전망 — 한 프레임 이동량 상한 배수. 어떤 경로로 위치가 바뀌든
    // runSpeedOf × 이 값을 넘지 못한다. 순간이동을 구조적으로 불가능하게 만든다.
    // 드리블(6.5 m/s)이 느린 선수의 주력보다 빠를 수 있어 하한을 함께 둔다.
    JUMP_CAP: 1.5,

    // 템포 — 공이 빠르게 움직이는 국면일수록 오프볼도 급해진다.
    // TEMPO_REF를 패스 속도(22)보다 낮게 잡으면 패스가 날아가는 내내 1로 포화돼
    // "완급"이 아니라 그냥 상수가 된다. 슛(30)까지 담되 드리블(6.5) 국면과는
    // 갈리도록 24로 둔다. 배율 폭도 1.9배는 과했다 — 라인 조정이 스프린트가 됐다.
    TEMPO_REF: 24,
    URGENCY_GAIN: 0.35, // urgency = 1 + GAIN × tempo → 1.00 ~ 1.35

    // 공 전개가 끝난 뒤 남기는 꼬리(ms). 여기서 재생이 끝난다.
    // 예전에는 아무도 안 기다리는 장식용 런이 재생을 끌어당겨 2액션 전개가 14.2초까지
    // 늘어났고, 그동안 전원이 목적 없이 떠다녔다(한 선수 총 이동 100m).
    TAIL_MS: 900,
    // 이 시각 이후로는 지원 런 선발을 멈추고 대형만 정리한다 — 끝난 공격에
    // 계속 침투 런을 넣으면 선수들이 앞뒤로 왔다 갔다 한다.
    SETTLE_MS: 400,

    // 역할이 바뀌면 목표가 30~50m 점프해 선수가 그 자리에서 유턴했다.
    // 이보다 크게 목표가 옮겨가면 한 번에 따라가지 않고 BLEND 동안 옮겨 간다 —
    // 갑작스러운 반전이 "판단을 바꾸는 움직임"으로 보이게.
    TARGET_JUMP: 15,
    TARGET_BLEND_MS: 600,
    // 지원 런 역할 최소 유지 시간 — 매 프레임 후보가 뒤바뀌며 깜빡이는 것을 막는다
    SUPPORT_HOLD_MS: 1200,

    // 방향 표시 안정화. 공을 막 받은 순간에는 이동/공 시선 판정이 교차하므로
    // 짧게 마지막 이동 방향을 유지하고, 한 프레임에 돌 수 있는 각도를 제한한다.
    FACE_MOVE_EPS: 0.008,
    FACE_MOVE_HOLD_MS: 260,
    FACE_BALL_EPS: 0.8,
    FACE_TURN_RAD_S: 4.2,

    // 강도는 상황 이름이 아니라 **목표까지 남은 거리**가 정한다.
    // 2m 어긋난 라인은 걸어서 맞추고, 16m 뒤처졌으면 전력으로 따라붙는다 —
    // 사람이 실제로 그렇게 움직인다. INTENT는 "다 왔을 때의 강도"가 되고,
    // 멀수록 거기서 전력까지 올라간다. (역습에서 수비·미드필더가 걷던 문제)
    EFFORT_NEAR: 3, // 이 거리 안이면 INTENT 그대로
    EFFORT_FAR: 18, // 이만큼 뒤처지면 전력

    // 라인 밀어올리기 — 공이 우리 진영에서 상대 진영으로 밀고 올라간 만큼
    // 팀 전체가 더 바짝 따라 올라간다. ROW_K는 정상 국면의 값이라
    // 역습에서 뒷선이 그대로 남아 "공격수만 뛴다"로 보였다.
    // 볼 스피드(tempo)로는 이걸 못 잡는다 — 드리블 역습은 공이 느리기 때문이다.
    PUSH_FULL: 30, // 공이 이만큼 전진하면 밀어올리기 최대
    PUSH_GAIN: 0.6, // 최대일 때 남은 간격의 몇 할까지 좁히나
    // 액션이 바뀔 때 전진량이 뚝 끊기면 라인 전체가 앞뒤로 튄다
    // (특히 뒤로 주는 패스). 팀이 함께 쓰는 값이라 여기서 한 번만 완화하면 된다.
    ADV_EASE: 2,

    // 복귀 질주 — 공보다 이만큼 뒤에 처진 수비 팀 선수는 자기 골문 쪽으로 전력으로 뛴다.
    //
    // 왜 따로 두는가: 라인 추종(ROW_K)은 "공이 전진한 만큼"의 비율이라, 코너킥 뒤
    // 역습처럼 전원이 80m 앞에 나가 있는 상황을 표현하지 못한다. 실측에서 포르투갈
    // 수비진이 5.5초 동안 0.9~4.4m밖에 안 움직여 서 있는 것처럼 보였다.
    // 게다가 defense.js의 마킹은 거리만 보므로, 공에서 70m 떨어진 수비수가 역시
    // 70m 떨어진 상대 풀백을 "마킹"하며 제자리에 묶여 있었다.
    //
    // 이 선수들은 판정 좌표(defPos)에도 묶지 않는다. 그 거리에서 압박 기여는
    // e^(−20/4) ≈ 0.007로 사실상 0이라, 화면이 자유롭게 움직여도 확률과 어긋나지 않는다.
    // (판정 코드는 건드리지 않는다 — 연출만의 문제다)
    RECOVER_BEHIND: 15, // 공보다 이만큼 뒤면 "따라잡아야 하는" 상태
    RECOVER_AHEAD: 12, // 공보다 이만큼 골문 쪽을 목표로 삼는다 (따라잡히지 않아도 방향이 맞다)
    RECOVERY_MARK_GOALSIDE: 4, // 복귀조 1명은 볼 소유자의 골사이드까지 붙어 압박한다

    // 압박조를 고를 때 공 뒤에 처진 수비수에게 매기는 거리 벌점(m).
    // 압박은 "맞설 수 있는 사람"이 하는 것이고, 공 뒤는 복귀 중이다.
    // 벌점이 없으면 뒤에 처진 선수가 압박조를 차지하고, 정작 맞설 수 있는
    // 수비수는 판정 좌표(액션 도착점)로 미리 달려가 서서 기다리게 된다.
    PRESS_BEHIND_PENALTY: 20,

    // 시선 — 정지 중인 선수가 전부 공만 노려보면 실감이 떨어진다(베타테스트 지적).
    // 공보다 이만큼 앞서 있는 공격수는 공이 아니라 골문 쪽을 본다.
    GAZE_AHEAD: 5,
    GAZE_MARK_R: 15, // 수비수가 마크 대상으로 삼아 바라보는 거리
    SCAN_DEG: 11, // 서 있는 동안 좌우로 훑어보는 폭(도)
    SCAN_HZ: 0.35,
  },

  // 슈팅 — 로지스틱 xG. z = B0 + B_DIST·lsFactor·D + B_ANG·θ + B_SKILL·(S_eff−SEFF0) + Σ B_BLOCK·e^(−d/R_BLOCK)
  SHOT: {
    // 중앙·무압박 발슈팅 앵커: 6/12/18/25yd = 0.42/0.18/0.09/0.04.
    // 거리·각도만으로 캘리브레이션하고, 골키퍼는 평균 선방 수준으로 이 기본값에 내재화한다.
    B0: -0.9702,
    B_DIST: -0.1111, // 거리 감쇠 /m
    B_ANG: 1.0659, // 골문 시야각 가산 /rad
    B_SKILL: 2.0, // 스킬 로그오즈 가중
    SEFF0: 0.7, // 스킬 중심점
    // 한 수비수가 슛 라인을 완전히 막아도 xG가 거의 0이 되지 않도록 보정한다.
    // d=0에서 odds ×0.47 (기존 ×0.08): 수비 차단은 반영하되 평균 GK 효과와 중복 감점하지 않는다.
    B_BLOCK: -0.75,
    R_BLOCK: 2.0, // 블로커 위협 반경 m
    // GK와 단독으로 맞선 상황: 수비수가 슛길을 막지 않을 때만 적용한다.
    // 12yd 중앙 1대1은 평균적으로 약 0.55 xG가 되도록 캘리브레이션.
    ONE_ON_ONE_BONUS: 1.7,
    // 골키퍼는 슛 방향을 비껴 차면 뚫리므로, 열린 슛이 GK가 슛 라인 위에 정확히
    // 서 있기를 요구해선 안 된다. 대신 GK가 골문 근처(깊이 GK_GOAL_DEPTH 안)에
    // 있고 슛길에 필드 수비수가 없으면 단독 찬스로 본다.
    ONE_ON_ONE_MAX_DIST: 28,
    ONE_ON_ONE_FULL_BONUS_DIST: 13, // 이 거리 안에선 BONUS 전액, 멀어질수록 FAR_BONUS로 감쇠
    ONE_ON_ONE_FAR_BONUS: 1.4,
    ONE_ON_ONE_MIN_XG: 0.4, // 진짜 열린 1대1은 마무리 1이어도 최소 40%
    ONE_ON_ONE_GK_GOAL_DEPTH: 9,
    // 조준 — 골문 안에서 어디를 겨누는가 (0 = 중앙, 1 = 포스트).
    //
    // 구석은 키퍼가 닿기 어렵지만 그만큼 빗나갈 위험도 크다. 두 효과를 각각 모델링하는
    // 대신 **차이만** 남긴다. 그리고 그 차이가 마무리 중립(SEFF0)에서 정확히 0이 되게 한다:
    //   · 평균 선수 — 구석이나 중앙이나 기대값이 같다 (이득과 위험이 상쇄)
    //   · 마무리가 좋은 선수 — 구석을 노려서 이득을 본다
    //   · 마무리가 나쁜 선수 — 구석을 노리면 손해, 중앙이 낫다
    //
    // 순보너스로 두지 않는 이유: xG 앵커는 전부 중앙 조준이라 어차피 안 흔들리지만,
    // 구석에 공짜 보너스를 주면 유저가 늘 구석을 노려 전체 xG가 캘리브레이션 위로 뜬다.
    // "평균에서 0"이면 조준은 보너스가 아니라 **선수 능력의 표현**이 된다.
    AIM_GAIN: 1.3,
    AIM_HEADER: 0.35, // 헤더는 코스를 고를 여유가 없다
    ONE_ON_ONE_BLOCK_CLEARANCE: 3,
    // 슛의 기본 xG는 그대로 두고, 이미 빗나갈 슛만 선방과 나눈다.
    SAVE_PATH_RADIUS: 2.4,
    SAVE_OF_MISS: 0.5,
    // 골문 코앞의 무방해 헤더는 일반 중거리 슛 식보다 높은 마무리 기회를 준다.
    HEADER_CLOSE_DIST: 7,
    HEADER_CLOSE_MIN_XG: 0.38,
    PENALTY_XG: 0.76, // D=11 페널티킥 특례 상수 (코어 fit 제외 — PK 상황 도입 시 사용)
    LS_RELIEF: 0.5, // 중거리 스탯의 거리 감쇠 완화 기울기: lsFactor = 1 − LS_RELIEF·(norm(중거리)−MID)
    LS_MIN: 0.7, // lsFactor 하한/상한 — 거리 감쇠 부호가 뒤집히지 않게
    LS_MAX: 1.2,
    B_AIR: 2.0, // 헤더 공중 듀얼 가중: z += B_AIR·(air_공격수 − air_수비수)
    HEAD_FIN: 0.3, // 헤더 스킬 = 골결 3 : 헤더 7 (데이터 요청 §3)
    // 로빙을 바로 슛으로 연결한 경우도 헤더 판정이지만, 크로스 헤더보다
    // 골결 비중을 높여 확률이 과도하게 떨어지지 않게 한다.
    LOB_HEAD_WEIGHT: 0.35,
    LOB_HEAD_AIR: 0.6,
  },

  // 패스 — 로그오즈, 리시버 근접 수비가 지배.
  // z = Z0 + B_LEN·L + B_PASS·(S_pass−0.70) + B_RECV·defScale·e^(−d_recv/R_RECV) + Σ B_LANE·defScale·e^(−d/R_LANE)
  PASS: {
    Z0: 2.933, // 무압박 앵커 절편 (8m→0.90, 20m→0.75, 30m→0.55)
    B_LEN: -0.0917, // 호 길이 감쇠 /m (곡선은 L이 길어져 자연 페널티)
    B_PASS: 1.5,
    B_RECV: -2.0, // 리시버 압박 (지배 레버)
    B_RECV_CLEAR: -1.1, // 패스 통로가 열렸으면 단순 근접 수비의 감점을 완화
    R_RECV: 4.0,
    B_LANE: -1.0, // 경로 압박 (수비수당, 보조)
    R_LANE: 3.0,
    CLEAR_LANE_R: 1.6, // 패스 중앙 구간에서 이 거리 안에 수비수가 있어야 '막힌 통로'
    // 짧은 패스는 실제 축구처럼 안정적이다. 수비 압박은 그대로 적용한다.
    SHORT_DIST: 12,
    SHORT_BONUS: 0.45,
    SHORT_CLEAR_FLOOR: 0.82, // 경로가 열린 일반 짧은 패스는 최소 82%
    // 로빙을 받은 선수가 곧바로 다시 패스하면 헤더가 일부 반영된다.
    // 비중을 낮춰 일반 패스보다 성공률이 지나치게 낮아지지 않게 한다.
    LOB_HEAD_WEIGHT: 0.25,
  },

  // 크로스 판정 기하: 측면(|y−40| ≥ WIDE_Y)에서 MIN_L 이상 날아와 박스 안(BOX_X, BOX_Y0~Y1)에 떨어지는 패스.
  // 크로스는 패스 스킬 대신 크로스 스탯 + 예측력 보정, 직후 슛은 헤더로 판정 (데이터 요청 §2·3·7)
  CROSS: { MIN_L: 18, WIDE_Y: 18, BOX_X: 102, BOX_Y0: 18, BOX_Y1: 62 },

  // 드리블 — 1v1 기하 지배, 능력치는 약한 modifier (근접 1v1 ≈ 0.5)
  // + 몸싸움 듀얼: z += B_BODY·e^(−d/R_DEF)·(body_공격수 − body_수비수), body = (몸싸움+균형)/2
  // 근접 수비에 막히는 것은 의도된 동작 — 계수 그대로 유지 (사용자 확인, 2026-07-20).
  // B_SURROUND: 최근접 수비수 **다음**부터 걸리는 포위 항. B_DEF의 40% 수준으로 둔다 —
  // 둘째 수비수는 공을 뺏으러 들어오는 사람이 아니라 빠져나갈 길을 막는 사람이다.
  // 2번째부터만 세므로 1v1 검증 앵커(간격 0.5/2/3/5 → 0.49/0.67/0.74/0.81)는 불변.
  DRIB: { Z0: 2.2, B_SKILL: 1.2, B_LEN: -0.06, B_DEF: -2.2, R_DEF: 3.0, B_BODY: 0.8, B_SURROUND: -0.9 },

  // 경합 게이트 (v2.2 신규) — "수비수가 없으면 드리블은 실패하지 않는다".
  //
  // 사용자 확정(2026-07-20): **드리블에만** 적용한다.
  //   드리블 = 내 발 밑의 공을 내가 몰고 가는 것. 뺏을 사람이 없으면 실패할 이유가 없다.
  //   패스   = 적용하지 않는다. 아무도 없어도 세게 차서 빠질 수 있다 (사용자 지적).
  //   슛     = 적용하지 않는다. 아무도 없어도 골결정력으로 빗나갈 수 있어야 한다.
  //
  //   p = 1 − (1 − p_raw) · gate(press),   gate(press) = 1 − e^(−press/KAPPA)
  //   press = 그 액션에 걸린 수비 압박 로그오즈의 크기(≥0)
  //
  // press=0(무압박) → gate=0 → p=1. 압박이 커질수록 gate→1이라 기존 산식으로 수렴한다.
  // 연속 함수라 "경계"가 없다 — 임계값으로 끊으면 확률이 튀어 체감이 이상해진다.
  // 근접(0.5m) 1v1은 49%로 사실상 그대로고, 멀어질수록 100%에 수렴한다.
  CONTEST: { KAPPA: 0.35 },

  // 수비 재배치 (defense.js) — 액션마다 수비수가 볼에 반응해 움직인다.
  // FM류 매치엔진과 같은 구조: 수비는 틱마다 조널/마킹/압박 목표를 재계산하고,
  // "볼 스피드 > 수비 이동력"일 때 공간이 열린다(수비 붕괴는 상수가 아니라 기하에서 창발).
  DEF: {
    REACT: 0.45, // 패스 인지 반응 시간 s — 이동 시간 예산 = 비행시간 + REACT
    SPD_MIN: 3.2, // 수비수 이동 속도 범위 m/s (주력+가속도 스탯으로 보간)
    SPD_MAX: 7.6,
    EFF: 0.6, // 이동 효율 — 최고속도 유지 불가·방향전환 손실 (공격 우위 아케이드 보정)
    MOVE_CAP: 9, // 액션 1개당 이동 상한 m — 긴 드리블 동안 수비가 무한 회복하는 것 방지
    K_LINE: 0.35, // 볼 전진량 대비 라인 후퇴 비율
    K_SIDE: 0.3, // 볼사이드 시프트 비율
    // 압박 **자리** 수. 인원 제한이 아니라 "볼 주변에 만드는 수비 자리"의 개수다.
    // 0번은 볼에 붙고 1번부터는 좌우로 벌려 커버한다(defense.js pressSlot).
    // 이 뒤 순번은 압박 대신 마킹·조널로 간다 — 실제 수비도 전원이 공에 달려들지 않고,
    // 나머지가 자기 상대를 잡는 편이 팀 전체로는 더 낫다.
    // 예전 값 2는 "전원이 한 점을 목표로 해서 몸이 겹치는" 문제를 가린 뚜껑이었다.
    PRESS_N: 3,
    FORMATION_RECOVERY_DISTANCE: 4,
    FORMATION_RECOVERY_BOOST: 1.28,
    RECOVERY_MARK_GOALSIDE: 4,
    SET_PIECE_X: 102,
    SET_PIECE_EDGE_Y: 14,
    SET_PIECE_PRESS_N: 1,
    SET_PIECE_MOVE_SCALE: 0.45,
    SET_PIECE_MARK_BLEND: 0.35,
    FORMATION_X: { DF: 96, MF: 82, FW: 70 },
    FORMATION_BALL_SHIFT: 0.12,
    RECOVERY_MOVE_CAP: 36,
    PRESS_R: 26, // 이 거리 안에 있어야 압박 가담
    GOALSIDE: 1.8, // 압박 위치: 볼과 자기 골문 사이 골사이드 오프셋 m
    COVER_DEPTH: 2.6, // 커버는 볼 압박자보다 이만큼씩 뒤에 (단계당)
    COVER_SPREAD: 4.5, // 커버는 볼-골문 축에서 좌우로 이만큼씩 (단계당)
    MARK_R: 10, // 이 거리 안의 공격수를 마킹
    MARK_GOALSIDE: 1.2, // 마킹 위치: 공격수의 골사이드 오프셋 m
    GK_X: 116.5, // GK 기본 x
    GK_TRACK: 0.25, // GK가 볼 y를 따라가는 비율
  },

  // 시퀀스 — 수비 붕괴의 주 메커니즘은 위치 재계산(위 DEF). FLOW는 잔여 "기세" 보정만 담당
  // (v2의 0.25 → 0.1로 축소: 연속 성공당 다음 액션 로그오즈 +FLOW, 상한 FLOW_MAX)
  SEQ: { FLOW: 0.1, FLOW_MAX: 1.0 },

  // 궤적 매칭 — 가우시안 커널, 1m 등간격 리샘플링, 임계 ≥90 완전 / 70~90 부분
  MATCH: { SIGMA: 3.0, RESAMPLE_M: 1.0, FULL: 90, PARTIAL: 70 },

  // 오프사이드 (offside.js) — 순수 기하. 계수가 아니라 규칙 상수라 밸런싱 대상이 아니다.
  // EPS: 동일선상 허용 오차 m. 실축 규칙과 같이 "같은 선 = 온사이드"라, 보드 조작의
  //      픽셀 단위 흔들림으로 억울한 오프사이드가 나오지 않게 하는 완충.
  OFFSIDE: { HALFWAY_X: 60, EPS: 0.15, PLAYER_RADIUS: 1.4 },

  // 시트(페이즈) 설계 UI — 가동범위 동심원 (sheets.js). 판정과 무관한 표시용.
  // 바깥 링 = 전력 100%, 안쪽 링 = 여유 70% (스프린트로 가긴 가는데 받을 준비는 안 되는 거리).

  // xT (xt.js) — 플레이 설계 점수. **판정과 완전히 독립**이라 밸런싱해도 앵커에 영향 없다.
  XT: {
    NX: 16, // 존 분할 (16×12 = 192존, 존 하나 7.5m × 6.67m)
    NY: 12,
    SHOT_MAX: 0.35, // 골문 바로 앞에서의 슛 시도 확률 상한
    SHOT_R: 11, // 슛 시도 확률의 거리 감쇠 상수 m
    MOVE_R: 14, // 이동 전이의 거리 감쇠 상수 m (짧은 패스가 흔하다)
    MOVE_MAX: 45, // 이 거리를 넘는 이동은 전이 후보에서 제외 m
    FWD_BIAS: 0.55, // 전진 편향 — 앞으로 가는 이동에 가중 (0이면 등방)
    // 액션 1회를 버티고 공을 지킬 확률. 이게 없으면(=1) 그리드가 평평해진다 — xt.js 주석 참고.
    // 낮출수록 "골문 근처"의 가치가 상대적으로 커지고 먼 곳의 전진 보상이 작아진다.
    RETAIN: 0.7,
    ITERS: 200, // 벨만 반복 상한
    EPS: 1e-7, // 수렴 판정
    // PlanScore 등급 경계 (xT 델타 합)
    GRADES: { S: 0.14, A: 0.07, B: 0.025, C: 0.0 },
  },
}

export const isLobPass = (action) =>
  action?.type === 'pass' && (action.passKind === 'lob' || action.passKind === 'lobThrough')

// 액션의 실제 진행 속도. 로빙은 type이 pass여도 체공 시간을 반영한다.
export const actionSpeed = (action) => {
  if (action?.type === 'pass' && Number.isFinite(action.passSpeed)) return action.passSpeed
  if (action?.type === 'pass' && action.passKind === 'through') return K.THROUGH.FALLBACK_GROUND
  if (action?.type === 'pass' && action.passKind === 'lobThrough') return K.THROUGH.FALLBACK_LOB
  if (isLobPass(action)) return K.SPEED.lob
  return K.SPEED[action?.type] ?? K.SPEED.pass
}
